export class HoseType {
    public id: number;
    public diameter: number;
    public length: number;

    constructor(id: number, diameter: number, length: number) {
        this.id = id;
        this.diameter = diameter;
        this.length = length;
    }
}